# Phase 2.1 cache

Runtime sample, MPB, state, tracer-parent, and event caches are written here
when the default CLI cache directory is used.  Cache files are disposable and
are never treated as source data.
