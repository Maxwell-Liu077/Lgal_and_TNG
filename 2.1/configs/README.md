# Phase 2.1 configuration

`src/utils/config.py` is the single source of scientific defaults. Runtime
paths are supplied by `scripts/run_analysis.py` or by callers of
`src.analysis.pipeline.run_phase21`.
