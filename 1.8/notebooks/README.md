# Notebooks

`01_phase18_analysis.ipynb` is the single interactive entry point. Its first
section reproduces the baseline result and its second section runs the
integrated inward-delivery extension while reusing the same caches and modules.

Reusable algorithms belong in `src/`, numerical outputs in `data/processed/`,
and saved figures in `results/figures/`.
