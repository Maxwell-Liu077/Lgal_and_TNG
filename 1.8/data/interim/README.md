# Intermediate data

`cache/` contains resumable sample catalogs, per-halo endpoint states, tracer
chunk scans, and parent-particle lookup chunks. Historical cache names and
fingerprints are retained so completed scans remain reusable.
