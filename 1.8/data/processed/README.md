# Processed scientific data

Final JSON metadata, per-halo CSV/NPZ arrays, binned NPZ statistics, normalized
NPZ statistics, and tracer-history NPZ arrays live directly in this directory.
The preserved `phase18_` and `phase181_` filename prefixes identify the two
historical output schemas without separate source or result trees.

PNG and PDF figures do not belong here.
