# Raw data

The raw IllustrisTNG simulation is intentionally external and read-only.
`Phase18Config.base_path` points to the TNG50-1 `output/` directory on the
compute system. Do not copy snapshots, group catalogs, or merger trees here.
