# Henriques-2015 cooling tables

This directory must contain all eight immutable `stripped_*.cie` tables from
the tagged Henriques-2015 L-Galaxies public release. Phase 2.1 has no fallback
cooling law; an alternate real-table directory may be supplied through
`run_analysis.py --cooling-table-dir`.
